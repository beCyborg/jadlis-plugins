---
name: batch-status
description: "Короткий статус установки Jadlis: четыре строки о том, какие батчи закрыты и какой идёт сейчас. Ничего не ставит и не меняет.\nTRIGGER when: user says \"статус батчей\", \"на каком я шаге\", \"что уже сделано\", \"jadlis статус\", \"batch status\", \"где я остановился\".\nDO NOT TRIGGER when: пользователь просит выдать или продолжить батч — это /jadlis-start:batch."
allowed-tools: Read, Bash
---

# Статус установки Jadlis

```
VAULT = ${user_config.VAULT_PATH}
```

Только чтение. Ничего не ставить, не создавать, не править — даже если по пробам видно,
что чего-то не хватает.

1. Прочитай `${CLAUDE_PLUGIN_ROOT}/skills/batch/references/детект-состояния.md` и выполни
   описанный там блок, подставив `VAULT` вместо `{VAULT_PATH}`.

2. Напечатай ровно пять строк — четыре батча и текущий:

```
1 Голос (Spokenly)      ✅ закрыт
2 Рабочее место         ✅ закрыт
3 Контекст и квартал    ⏳ метрик 4 из 10, целей квартала 0 из 2
4 Ресерч-стек           ⬜ не начат

Твой батч сейчас: 3 → напиши `JADLIS-BATCH 3`
```

Незакрытый батч показывай **цифрами из проб**, а не словами «не завершён». Прозой пробы
не пересказывай, Bash-вывод целиком не показывай.

3. Журнал расходится с пробами — скажи об этом одной строкой и не трогай журнал:
   переписывает его драйвер, не этот скилл.
